# 缓存 
预计:两个小时
实际: 4.48个小时,最主要的多线程、hutool包,线程池有很多不知道了,还有范型等知识. 而且自己还把一些他的错误逻辑修改了


# 秒杀活动
预计:2个小时
实际:8个多小时,最主要的就是多线程、并发问题,线程池原因. 全局id好解决,秒杀下单,用乐观锁进行实现(最主要的就是,一开始jmx不太会用,虽然现在也不会,但是知道怎么做的). 一人一锁问题, 多种方法的不断改进, 首先就是基础的锁, 然后这个锁存在一些问题(每一个人只能解自己的锁, 这时候要用userid进行实现,同时并发问题的细粒度问题,从方法加锁,到userID加锁, spring代理事务的问题,要手动代理).  然后基于这个实现了分布式锁的问题. 为什么要采用分布式锁的,主要的就是不同进程(注意是不同进程),他们的不同线程仍然会造成冲突,最主要的缺乏一个统一管理锁

